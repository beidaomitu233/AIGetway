# 轻享 AI 企业 AI 中台产品需求说明书

## 文档信息

| 项目 | 内容 |
|---|---|
| 产品名称 | 轻享 AI（Light AI） |
| 产品定位 | 面向企业内部应用的统一 AI 接入、模型调度与用量治理中台 |
| 文档版本 | V2.0 重构评审稿 |
| 文档状态 | 新产品基线，待业务与技术评审 |
| 更新日期 | 2026-09-08 |
| 目标读者 | 产品、架构、研发、测试、运维、安全及企业应用负责人 |
| 权威决策 | 企业应用是平台管理与治理的第一业务对象；平台不面向 C 端售卖 AI 服务 |
| 旧版处置 | 原 V1.0 PRD 及其前后端、数据库、审查和修复过程文档自本版建立后失效 |

## 版本记录

| 版本 | 日期 | 状态 | 说明 |
|---|---|---|---|
| V2.0 | 2026-09-08 | 重构评审稿 | 以企业应用接入为主线重新定义产品、流程、权限、数据、页面、接口与验收要求 |

# 1 产品背景与目标

## 1.1 背景

企业内多个业务系统接入大模型时，通常分别维护供应商地址、模型名和 API Key，并各自实现限流、重试、降级、成本统计与审计。这种方式会造成凭证散落、接入协议不一致、模型切换成本高、上游额度利用率低、成本归属不清以及故障处置无法统一。随着接入应用和模型数量增加，单个业务团队很难独立处理多供应商差异、容量限制和安全要求。

轻享 AI 要成为企业应用与外部 AI 服务之间的统一中转站。企业应用只连接轻享 AI 的统一地址并使用平台签发的应用密钥，不直接持有供应商 Key；平台根据应用权限、额度、RPM、TPM、模型路由和渠道健康状态完成请求准入、模型调度、负载均衡、故障切换、用量结算与调用审计。

本版重构的核心变化是把“应用”提升为产品第一业务对象。渠道、供应商 Key、上游模型和路由属于平台基础资源，最终都服务于某个应用的可用模型、接入凭证、容量边界和费用边界。管理人员应能从应用详情完成接入配置并持续看到该应用的调用、Token、费用、RPM、TPM与异常情况。

## 1.2 产品定位

轻享 AI 是企业内部的 AI API Gateway 与治理中台，提供统一兼容接口、应用级访问控制、上游渠道与 Key 池、虚拟模型、路由与容灾、配额和预算、调用观测及审计能力。平台主要服务企业内部应用、部门系统、自动化任务和研发测试环境。

本产品参考 New API 的统一网关、渠道、令牌、配额和日志管理思路，但业务主体由“平台用户”替换为“企业应用”。平台不提供面向 C 端的注册、充值、订阅、邀请、兑换码、返利和公开售卖功能；金额用于企业内部成本核算与预算控制，不代表用户余额或支付账户。

## 1.3 产品目标

| 目标 | 目标结果 | 验证口径 |
|---|---|---|
| 快速接入 | 已配置上游资源时，应用负责人可在一个连续流程中创建应用、授权模型、配置额度并获得接入密钥 | 从创建应用到首次成功调用，全程无需接触上游 Key |
| 统一协议 | 业务应用使用稳定的统一 API，切换渠道或真实模型时无需修改业务代码 | 虚拟模型名称和接口协议保持稳定，路由变更只在中台完成 |
| 统一资源 | 供应商渠道、上游模型及多个 Key 集中管理和健康检测 | 上游 Key 不进入业务应用，平台可按优先级与权重分配流量 |
| 应用治理 | 每个应用独立配置模型范围、Token 额度、金额预算、RPM、TPM和有效期 | 任一请求均能追溯到应用和应用密钥，并在准入前执行边界校验 |
| 稳定调用 | 上游限流、超时或故障时，在明确预算内自动重试、换 Key 或切换渠道 | 调用记录完整展示每次尝试和最终路径，流式响应不拼接不同路径 |
| 成本透明 | 按应用、密钥、虚拟模型、真实模型、渠道和时间统计 Token 与成本 | 成本使用请求发生时的价格快照，调整价格不改写历史数据 |
| 安全可审计 | 上游 Key 与应用密钥全生命周期受控，关键配置操作可追踪 | 密钥只显示一次或始终掩码，日志和导出不含敏感原文 |

## 1.4 本期范围

V2.0 首期围绕文本大模型 Chat Completions 建立可交付闭环，包括：企业应用管理、应用接入密钥、应用模型权限、Token 与金额预算、RPM/TPM、供应商与渠道、渠道多 Key 池、上游模型、虚拟模型、路由负载均衡、重试与故障切换、统一 API、调用日志、用量与成本、配置生效、管理权限、审计和浅色管理后台。

平台交付形态以集中部署的 Standalone Gateway 和管理后台为主。Java SDK 可以作为调用统一 API 的便捷客户端，但不得承载平台权威配置。已有 Embedded 与 Local Runtime 能力仅作为技术资产评估，不进入 V2.0 首期验收基线，后续是否保留由架构评审决定。

## 1.5 非本期范围

以下能力不进入 V2.0 首期：面向 C 端的用户注册、公开登录、充值、订阅、支付、兑换码、邀请返利和商业账单；Prompt 管理、知识库、RAG、工作流、Agent Runtime、模型训练；图片、音频、视频等多模态业务接口；终端用户聊天产品；跨企业 SaaS 多租户运营；自动采购供应商额度；跨币种财务结算。

管理后台仍需要企业身份认证和角色权限。这里的“无 C 端用户体系”不等于匿名管理，平台管理员和应用负责人必须来自企业可信身份源或部署方明确配置的管理身份。

## 1.6 产品原则

- 应用优先：所有接入、权限、额度、费用和观测入口都能回到应用。
- 密钥隔离：应用只持有平台密钥，平台集中保护上游凭证。
- 稳定抽象：业务调用虚拟模型，真实模型和渠道可在平台内调整。
- 先准入后调用：身份、权限、预算和频率限制在外部调用前完成。
- 原子结算：并发预占、取消、超时、异常和成功都必须单次结算并释放。
- 配置可追溯：变更有版本、操作者、时间和影响范围，运行请求绑定确定快照。
- 默认安全：身份、权限、密钥、网络目标和敏感日志均采用默认拒绝。
- 克制设计：页面突出业务对象、状态和操作，不使用无意义卡片、标签和说明文字。

# 2 系统角色

## 2.1 角色定义

| 角色 | 定义 | 核心职责 | 默认数据范围 |
|---|---|---|---|
| 系统管理员 | 企业 AI 中台的配置与安全负责人 | 管理成员权限、应用、渠道、上游 Key、模型、路由、全局设置和审计 | 全平台 |
| 平台运维 | 负责网关稳定性和日常运行 | 查看全局运行、检测渠道、处理故障、调整运行状态、查看调用与用量 | 全平台运行数据；不查看密钥原文 |
| 应用负责人 | 企业业务应用的技术负责人或开发者 | 创建或维护获授权的应用、签发应用密钥、配置应用额度与模型权限、查看本应用调用 | 被授权应用 |
| 审计查看者 | 安全、合规、成本或管理人员 | 查看配置变更、调用统计、费用和审计记录，按授权导出 | 授权范围内只读 |
| 业务应用 | 使用应用密钥调用统一 API 的系统主体 | 发起模型请求、接收响应、处理业务错误和重试建议 | 当前密钥绑定的单一应用 |
| 平台调度器 | 网关内部自动化主体 | 准入、限流、路由、选 Key、调用上游、结算、记录和健康更新 | 系统内部 |

企业身份与角色映射由部署方配置。一个人员可以拥有多个角色，权限取并集，数据范围取角色授权范围的并集；敏感权限仍需显式授予。业务应用身份与管理人员身份完全分离，应用密钥不能登录管理后台，管理会话也不能直接调用业务 API。

## 2.2 典型场景

| 场景 | 发起者 | 完成结果 |
|---|---|---|
| 新应用接入 | 应用负责人 | 获得统一地址、应用密钥和可用虚拟模型，完成首次调用 |
| 增加应用预算 | 应用负责人或系统管理员 | 新预算对后续请求生效，历史消耗与调整记录保留 |
| 轮换应用密钥 | 应用负责人 | 新密钥生效，旧密钥在宽限期结束或立即撤销后失效 |
| 增加上游 Key | 系统管理员 | Key 加密保存并进入渠道负载均衡，不向应用暴露 |
| 模型迁移 | 系统管理员 | 虚拟模型不变，路由切换至新的真实模型或渠道 |
| 上游故障 | 平台调度器、平台运维 | 在恢复预算内换 Key 或切换渠道，失败时返回统一错误并完整记录 |
| 成本复盘 | 应用负责人、审计查看者 | 按应用和模型查看 Token、金额、趋势、异常和明细 |

# 3 核心业务术语与模型

## 3.1 术语

| 术语 | 定义 |
|---|---|
| 应用 Application | 企业内部接入 AI 的最小治理单元，承载负责人、状态、模型权限、预算、RPM、TPM和调用归属 |
| 应用密钥 Application Key | 平台签发给单一应用的 API 访问凭证，可用于环境隔离和轮换；完整值只在创建或轮换时显示一次 |
| Token 额度 | 某应用在指定周期或生命周期内可消耗的输入与输出 Token 总量上限 |
| 金额预算 | 某应用在指定周期或生命周期内允许产生的模型成本上限，按明确币种和价格快照计算 |
| RPM | 应用或应用密钥每分钟允许通过的请求数上限 |
| TPM | 应用或应用密钥每分钟允许消耗的 Token 数上限，准入使用估算值，完成后按实际值结算 |
| Provider | 模型服务商或协议类型，例如 OpenAI、Anthropic、Gemini、DeepSeek 或 OpenAI Compatible |
| 渠道 Channel | 一条可调用的上游服务连接，包含 Provider 类型、Base URL、可用模型、网络参数和 Key 池 |
| 渠道 Key | 渠道下的一份上游凭证，拥有启停、权重、容量和健康状态；一个渠道可配置多份 Key |
| 上游模型 Upstream Model | 某渠道真实提供的模型及能力、上下文、价格和状态 |
| 虚拟模型 Virtual Model | 面向企业应用暴露的稳定模型名称，可映射一个或多个渠道上的真实模型 |
| 路由候选 Route Candidate | 虚拟模型下的真实调用路径，绑定渠道、上游模型、优先级、权重和适用条件 |
| 配置版本 | 渠道、模型、路由等全局运行配置的一致性快照 |
| Trace | 一次业务应用请求的全链路记录 |
| Attempt | Trace 内对某个渠道、真实模型和渠道 Key 的单次外部调用尝试 |
| 用量账本 | 记录应用、密钥、模型、渠道、Token 和金额变化的不可覆盖明细 |

## 3.2 核心关系

一个应用可以拥有多份应用密钥，所有密钥共享应用级模型权限与预算，并可配置更严格的密钥级有效期、IP 范围和限额。应用只能调用被授权的虚拟模型。虚拟模型通过路由候选映射到一个或多个渠道和上游模型；渠道内部再从可用渠道 Key 中选择一份凭证发起调用。

一次请求必须唯一归属于一个应用和一份应用密钥。平台在外部调用前检查应用状态、密钥状态、IP、模型权限、Token 额度、金额预算、RPM 和 TPM；通过后建立 Trace、锁定配置快照并预占容量。调用完成后按真实 Usage 和价格快照结算，任何失败、取消和超时路径都释放未使用预占。

# 4 主要业务流程

## 4.1 应用接入主流程

该流程从应用负责人提出接入开始，以业务应用通过统一 API 完成首次真实调用结束。上游渠道和虚拟模型可以由平台预先配置，也可以作为接入前置任务补齐。

| 步骤 | 执行者 | 动作 | 系统结果 | 异常处理 |
|---|---|---|---|---|
| 1 | 应用负责人 | 创建应用，填写名称、唯一编码、负责人、部门、环境和说明 | 应用进入启用或待完善状态，生成应用 ID | 编码重复时拒绝并保留输入 |
| 2 | 应用负责人 | 选择允许调用的虚拟模型 | 建立应用模型权限 | 不可用或未发布模型不能授权 |
| 3 | 应用负责人 | 设置 Token 额度、金额预算、RPM、TPM、周期和生效时间 | 生成应用治理策略版本 | 额度或周期非法时不保存 |
| 4 | 应用负责人 | 创建应用密钥，设置名称、有效期和可选 IP 白名单 | 完整密钥只显示一次，后台只保存摘要 | 未复制关闭后不可再次查看，只能轮换 |
| 5 | 系统 | 生成接入地址、可用模型和 SDK/cURL 示例 | 接入信息与当前应用策略一致 | 无可用模型时明确提示前置未完成 |
| 6 | 业务应用 | 使用应用密钥调用 `/v1/models` 和 Chat API | 返回授权模型和模型响应，形成 Trace 与用量 | 统一错误包含 request_id，不暴露上游凭证 |

## 4.2 上游资源接入流程

系统管理员创建渠道，选择 Provider 协议并配置 Base URL、网络超时和可用模型。随后添加一份或多份渠道 Key，Key 经加密保存并通过最小请求进行连接测试。平台导入或人工维护上游模型能力与价格，只有连接、模型能力和价格校验通过的资源才能进入已发布路由。

渠道 Key 测试失败不会泄露上游响应中的敏感内容。认证失败的 Key 标记为不可用并退出新请求选择；短时限流或网络故障更新健康状态，由调度策略决定冷却和恢复。人工启停立即影响新请求，已有请求按原快照完成或失败。

## 4.3 虚拟模型与路由发布流程

系统管理员创建稳定的虚拟模型名称并添加路由候选。候选按优先级分层，同优先级内按权重分流；只有当前优先级无可用候选或满足故障切换条件时才进入下一优先级。配置保存为草稿，系统校验引用、能力、价格、渠道 Key、应用影响和运行实例兼容性后发布为不可变版本。

发布完成后，新请求使用新版本，运行中的请求继续使用开始时锁定的版本。发布失败保留上一活动版本，不允许部分实例静默使用不一致配置。回滚通过重新激活历史完整版本实现，并形成新的变更记录。

## 4.4 运行调用流程

| 阶段 | 核心规则 | 主要输出 |
|---|---|---|
| 接入鉴权 | 解析应用密钥摘要，校验状态、有效期、应用状态和来源 IP | ApplicationContext |
| 模型授权 | 将请求模型解析为虚拟模型，校验应用及密钥模型范围 | 已授权虚拟模型 |
| 额度准入 | 原子校验并预占 Token 额度、金额预算、RPM 和 TPM | Reservation 或统一拒绝错误 |
| 路由选择 | 依据活动配置版本、优先级、权重、健康、能力和容量选择候选 | 渠道与上游模型 |
| Key 选择 | 在渠道可用 Key 中按优先级、权重和剩余容量选择 | 单一上游凭证句柄 |
| 外部调用 | Adapter 完成协议转换和一次外部请求 | 响应、Usage 或分类错误 |
| 恢复决策 | 在请求总时限和恢复预算内执行同 Key 重试、换 Key 或切换候选 | 下一 Attempt 或最终失败 |
| 结算记录 | 按实际 Token、价格快照和最终路径结算，释放剩余预占 | Trace、Attempt、Usage Ledger |
| 返回应用 | 输出统一响应、request_id、虚拟模型名和必要用量信息 | 非流式 JSON 或流式 SSE |

## 4.5 配额调整与续期流程

应用负责人可以增加、减少或重置应用预算，也可以修改 RPM/TPM 和周期。所有变更以“调整单”记录变更前值、变化量或新值、原因、操作者和生效时间。增加额度可以立即或预约生效；减少后的上限若低于本周期已用量，系统不回滚历史消费，立即拒绝后续新请求。

“续期”表示为新的周期补充 Token 额度和金额预算，或延长应用密钥有效期，不得通过修改历史用量实现。手工重置属于独立高风险操作，需要二次确认和审计。RPM/TPM 变更对新准入立即生效，当前窗口已累计值继续保留到窗口结束。

## 4.6 故障与恢复流程

平台将错误分为请求错误、鉴权与权限、配额与限流、路由与能力、上游认证、上游限流、上游超时、上游服务异常以及平台内部异常。只有明确可恢复的上游错误进入自动恢复。流式响应首个业务内容块提交前可以更换路径；首个业务内容块提交后不得切换模型或拼接其他渠道输出，发生错误时发送统一流内错误并结束。

# 5 功能范围概览

| 模块 | 业务能力 | 主要页面 | 上下游关系 |
|---|---|---|---|
| 总览 | 平台健康、请求、Token、成本、应用和渠道异常 | 运行总览 | 汇总应用、调用和渠道数据 |
| 应用中心 | 应用、应用密钥、模型授权、额度、RPM/TPM、接入信息 | 应用列表、应用详情、创建/编辑、密钥、配额调整 | 调用准入和成本归属的中心 |
| 渠道中心 | Provider 连接、渠道、多 Key、健康检测、上游模型 | 渠道列表、渠道详情、Key 管理、模型同步 | 为虚拟模型提供真实资源 |
| 模型与路由 | 上游模型、虚拟模型、候选、优先级、权重和发布 | 模型目录、虚拟模型详情、路由编辑、发布 | 向应用提供稳定模型名 |
| 调用网关 | OpenAI 兼容接口、流式、鉴权、准入、路由和恢复 | 无独立业务页面 | 承接业务应用流量 |
| 调用观测 | Trace、Attempt、错误、延迟、Token、费用 | 调用记录、调用详情 | 支撑定位与审计 |
| 用量与成本 | 应用预算、用量趋势、分组统计、调整账本 | 用量分析、成本分析、额度流水 | 支撑内部核算和治理 |
| 系统管理 | 企业身份、角色、审计、运行参数、配置版本 | 成员与权限、审计日志、系统设置、发布记录 | 保护平台配置和操作 |
| 开发接入 | 接入地址、可用模型、示例、在线测试 | 开发接入 | 帮助应用快速完成首调 |

# 6 角色权限

## 6.1 功能权限矩阵

| 能力 | 系统管理员 | 平台运维 | 应用负责人 | 审计查看者 |
|---|---|---|---|---|
| 查看全局总览 | 是 | 是 | 仅所属应用摘要 | 按授权查看 |
| 创建、编辑、停用应用 | 是 | 否 | 授权时可创建和编辑所属应用 | 否 |
| 管理应用密钥 | 是 | 否 | 所属应用 | 否 |
| 调整 Token/金额/RPM/TPM | 是 | 授权时可操作 | 所属应用且受上限约束 | 否 |
| 配置应用模型权限 | 是 | 否 | 在授权模型集合内操作 | 否 |
| 管理渠道和上游 Key | 是 | 可检测和启停 | 否 | 只读且不见敏感字段 |
| 管理上游模型、虚拟模型和路由 | 是 | 可检测和临时摘除 | 否 | 只读 |
| 发布或回滚运行配置 | 是 | 授权时可执行 | 否 | 只读 |
| 查看调用记录 | 全部 | 全部 | 所属应用 | 按授权范围 |
| 查看用量与成本 | 全部 | 全部 | 所属应用 | 按授权范围 |
| 查看审计 | 是 | 运行类记录 | 所属应用操作记录 | 按授权范围 |
| 管理成员、角色和系统设置 | 是 | 否 | 否 | 否 |

所有权限必须同时在页面入口、按钮、API 和数据查询层执行。隐藏按钮不能代替后端鉴权；通过直接 URL 或构造应用 ID 访问其他应用时返回 403，不能用 404 掩盖审计所需的越权事实。渠道 Key 原文不授予任何日常查看权限，系统管理员也只能在写入时提交新值。

## 6.2 数据范围

应用负责人通过显式应用成员关系获得数据范围。应用成员至少区分负责人和只读成员，是否增加“额度管理员”作为细粒度角色列入待确认事项。平台运维默认能查看全局运行数据和掩码后的配置，但不能修改业务归属、查看密钥原文或扩大应用预算。

# 7 数据与状态

## 7.1 核心实体

| 实体 | 关键字段 | 关系与生命周期 | 敏感级别 |
|---|---|---|---|
| application | id、code、name、department、owner、environment、status、version | 一对多密钥、策略、权限和调用 | 内部 |
| application_member | application_id、subject_id、role | 决定应用数据范围 | 内部 |
| application_key | id、application_id、name、key_prefix、key_digest、expires_at、ip_allowlist、status、last_used_at | 完整值不持久化；轮换生成新记录 | 高敏 |
| application_quota_policy | application_id、token_limit、amount_limit、currency、rpm、tpm、period_type、period_start、period_end、version | 每个应用一个当前策略，历史版本保留 | 内部 |
| quota_adjustment | application_id、dimension、before_value、delta、after_value、reason、effective_at、operator | 只追加，不覆盖 | 内部审计 |
| application_model_permission | application_id、virtual_model_id、enabled、constraints | 控制应用可见和可调用模型 | 内部 |
| provider | id、type、name、adapter_type、status | Provider 类型与协议元数据 | 普通 |
| channel | id、provider_id、name、base_url、priority、weight、timeouts、status、version | 一对多渠道 Key 和上游模型 | 内部 |
| channel_credential | id、channel_id、name、secret_ciphertext、key_id、priority、weight、rpm、tpm、status、health | Key 原文加密后不可读出 | 最高敏感 |
| upstream_model | id、channel_id、model_id、capabilities、context_window、prices、currency、status、version | 绑定具体渠道 | 内部 |
| virtual_model | id、code、display_name、capabilities、status、version | 面向应用暴露 | 普通 |
| route_candidate | virtual_model_id、channel_id、upstream_model_id、priority、weight、status | 构成运行路由 | 内部 |
| config_snapshot | snapshot_no、content_hash、status、created_by、activated_at | 不可变运行配置 | 内部 |
| request_trace | request_id、application_id、application_key_id、virtual_model_id、status、timings、usage、cost、snapshot_no | 一次业务请求 | 受控 |
| request_attempt | trace_id、candidate、channel、credential_id、status、error、timings、usage | 一次上游尝试；凭证仅存内部 ID | 受控 |
| usage_ledger | application、key、trace、token_delta、amount_delta、currency、price_snapshot、occurred_at | 用量与成本唯一事实来源 | 受控 |
| budget_reservation | request_id、application_id、reserved_tokens、reserved_amount、expires_at、status | 支撑并发预占与故障恢复 | 内部 |
| audit_log | operator、action、target、before_digest、after_digest、result、request_id、created_at | 只追加并按策略留存 | 审计 |

关键唯一约束至少包括：应用 code 全局唯一；应用密钥摘要唯一；同一渠道内 Key 名称唯一；同一渠道内上游 model_id 唯一；虚拟模型 code 全局唯一；同一虚拟模型下不得重复绑定同一渠道与上游模型；request_id 全局唯一；同一账本业务事件只能结算一次。

## 7.2 状态定义

| 对象 | 状态 | 含义与允许操作 |
|---|---|---|
| 应用 | ACTIVE | 可签发密钥并接收请求 |
| 应用 | DISABLED | 拒绝新请求，可查看历史和调整后重新启用 |
| 应用 | ARCHIVED | 终态归档，不可调用和新增密钥，历史保留 |
| 应用密钥 | ACTIVE | 在有效期和策略范围内可调用 |
| 应用密钥 | DISABLED | 人工停用，可恢复 |
| 应用密钥 | EXPIRED | 到达有效期自动失效 |
| 应用密钥 | REVOKED | 永久撤销，不可恢复 |
| 渠道配置 | ACTIVE / DISABLED | 控制是否参与新请求路由 |
| 渠道健康 | HEALTHY / DEGRADED / UNAVAILABLE / UNKNOWN | 由检测和真实调用更新，不直接覆盖人工配置状态 |
| 渠道 Key 配置 | ACTIVE / DISABLED | 控制是否可被选择 |
| 渠道 Key 健康 | HEALTHY / RATE_LIMITED / INVALID / UNAVAILABLE / UNKNOWN | 反映认证、限流和连接状态 |
| 模型 | ACTIVE / DISABLED | 控制是否可授权或路由 |
| 配置版本 | DRAFT / VALIDATING / READY / ACTIVATING / ACTIVE / FAILED / SUPERSEDED | 描述草稿到活动版本的完整过程 |
| Trace | RUNNING / SUCCEEDED / FAILED / CANCELLED | 请求主状态 |
| Reservation | RESERVED / SETTLED / RELEASED / EXPIRED | 配额预占状态，终态操作幂等 |

配置状态与健康状态必须分开。健康失败不能把人工禁用改成启用，人工启用也不能把认证无效的 Key 视为健康。应用和密钥禁用对新请求立即生效；已通过准入的请求按照开始时的上下文完成并正常结算。

# 8 关联系统与集成

| 系统 | 方向 | 主要数据 | 鉴权与失败原则 |
|---|---|---|---|
| 企业身份源 | 身份源 → 管理后台 | 用户标识、显示名、组织、角色或组 | 支持 OIDC/SAML/网关头的具体方式待部署确认；不可用时默认拒绝管理访问 |
| 企业业务应用 | 双向 | 模型请求、流式响应、Usage、错误 | 应用密钥 Bearer 认证，TLS，支持 IP 白名单 |
| AI Provider | 双向 | 统一请求转换后的上游请求、响应、Usage、错误 | 渠道 Key，目标地址白名单和 SSRF 防护，禁止跟随到非允许目标 |
| Secret/KMS | 平台 ↔ 密钥系统 | 上游凭证密文或引用、加密 Key ID | 失败时不回退明文；轮换与读取审计 |
| Redis/共享状态 | 平台 ↔ 共享存储 | RPM、TPM、容量、预占、熔断、短期健康 | 不可用时准入 fail-closed，不退化为单实例独立计数 |
| 关系数据库 | 平台 ↔ 数据库 | 应用、配置、版本、Trace、账本和审计 | 迁移版本校验；写事务和唯一约束保证幂等 |
| 可观测平台 | 平台 → 指标/日志/Trace 系统 | 非敏感指标、告警信号、Trace 关联 ID | 默认不发送消息正文、密钥、认证头和完整 IP |

# 9 详细功能需求

## 9.1 运行总览

运行总览面向系统管理员和平台运维，应用负责人进入时只展示所属应用汇总。页面路径为 `/overview`，首屏按照“风险与容量、核心用量、趋势、重点应用、渠道健康”的顺序组织。

| 区域 | 内容 | 关键交互 |
|---|---|---|
| 时间与范围 | 最近 1 小时、24 小时、7 天、30 天和自定义；应用负责人锁定自身范围 | 切换条件统一刷新所有图表并在 URL 保存 |
| 风险摘要 | 不可用渠道、无可用路由、预算耗尽应用、限流应用、失败率异常 | 点击进入对应筛选列表 |
| 核心指标 | 请求数、成功率、输入/输出 Token、金额、RPM 峰值、TPM 峰值、P95 延迟 | 显示口径和更新时间 |
| 趋势 | 请求、Token、金额、成功率和延迟趋势 | 支持粒度自适应和图例开关 |
| 应用排行 | 按请求、Token、金额和失败率查看重点应用 | 进入应用详情 |
| 渠道健康 | 渠道和 Key 健康分布、最近失败 | 进入渠道详情或调用记录 |

空范围显示字段完整的零值和引导，不将“无数据”显示成错误；聚合服务不可用时显示明确错误并允许刷新，不能返回成功的空集合掩盖故障。

## 9.2 应用中心

应用中心是管理后台的首要模块。所有接入配置、额度与运行数据均能从应用详情到达，避免用户在渠道、限流、Token 和用量页面之间自行拼接业务关系。

### 9.2.1 应用列表

页面路径为 `/applications`。列表支持按名称或编码搜索，按状态、部门、负责人、环境和预算状态筛选，默认按最近调用时间降序。

| 列 | 说明 |
|---|---|
| 应用 | 名称、唯一编码和环境；点击进入详情 |
| 负责人/部门 | 企业组织信息 |
| 状态 | ACTIVE、DISABLED 或 ARCHIVED |
| 可用模型 | 已授权并可路由的虚拟模型数量 |
| 密钥 | 活动密钥数量和最近使用时间 |
| Token | 当前周期已用/上限及百分比 |
| 金额 | 当前周期已用/预算、币种及百分比 |
| RPM/TPM | 当前限制和最近峰值 |
| 运行 | 最近 24 小时请求数、成功率和最后调用时间 |

系统管理员可以新建、编辑、禁用和归档应用。应用负责人仅操作有权限的应用。禁用前展示受影响密钥和最近调用；归档要求应用已禁用且不存在运行请求，归档后不允许恢复为活动状态。

### 9.2.2 应用创建与编辑

页面路径为 `/applications/new` 和 `/applications/{id}/settings`。创建流程使用单页分区表单，字段包括应用名称、唯一编码、部门、负责人、环境、说明、初始模型权限、Token 额度、金额预算、币种、周期、RPM、TPM和启用状态。应用 code 创建后不可修改，用于日志、SDK 配置和内部关联。

Token 额度和金额预算都支持“无限制”，但系统必须明确显示风险；至少配置 RPM 或 TPM 中的一项作为应用流量保护的建议规则，是否强制由待确认事项决定。保存前展示配置摘要，保存成功进入应用详情的接入引导。

### 9.2.3 应用详情

页面路径为 `/applications/{id}`。页头只展示名称、code、状态、负责人以及“编辑”“禁用/启用”主操作。正文使用页签组织：概览、接入密钥、可用模型、额度与速率、调用记录、用量成本、成员与审计。

概览页签提供接入状态检查清单、API Base URL、当前活动密钥、可用模型、额度剩余、RPM/TPM 和最近调用。若应用尚未完成首调，显示最短接入向导；完成后改为运行摘要，不继续占用大面积引导区域。

### 9.2.4 应用密钥

密钥页签展示名称、掩码前缀、状态、创建时间、有效期、IP 白名单、最近使用和操作者。创建时可以指定名称、有效期、IP/CIDR 白名单，以及可选的密钥级 RPM/TPM 和虚拟模型子集；密钥级限制只能比应用级策略更严格。

完整密钥在创建和轮换成功响应中只显示一次。页面提供复制和下载为临时配置的动作，但离开一次性弹窗后不能再次查看。系统只持久化不可逆摘要和非敏感前缀。轮换生成新密钥，支持立即撤销旧密钥或设置不超过待确认上限的宽限期；撤销不可恢复，需要二次确认。

### 9.2.5 可用模型

可用模型页签列出虚拟模型名称、能力、上下文、应用自定义显示名、状态和最近调用。授权模型时只展示已发布且至少有一个可用路由的虚拟模型。取消授权前展示最近调用和受影响密钥；取消后新请求立即返回模型无权限，运行中请求不受影响。

应用可以为虚拟模型配置可选的请求参数上限，例如最大输出 Token 和是否允许流式，但不能突破虚拟模型和上游候选的能力边界。应用不选择具体渠道 Key，也看不到上游凭证。

### 9.2.6 额度与速率

额度页签集中展示 Token、金额、RPM、TPM，禁止拆散到多个无关联页面。每个维度显示当前策略、当前周期、已用、已预占、剩余、使用率、下次重置时间和最近调整。

应用负责人可以发起增加、减少、续期和策略修改。操作表单必须要求原因并显示生效后的预览。金额使用 decimal 定点值和 ISO 4217 币种；Token 使用整数。降低上限到已用值以下时显示“保存后立即停止新请求”的明确警告。所有调整写入只追加流水，并在并发版本冲突时要求重新加载。

RPM 和 TPM 使用共享原子窗口。请求准入时同时检查应用和密钥限制，任一层达到上限即拒绝。响应返回标准限流状态及可重试时间，不暴露其他应用的容量。

### 9.2.7 应用成员与审计

应用负责人可以查看应用成员及其角色，成员来源于企业身份系统。成员变更是否由平台直接维护或由外部组织系统同步列入待确认。应用审计按时间显示密钥创建/轮换/撤销、模型授权、额度调整、状态变更和成员变更，不包含任何密钥原文。

## 9.3 渠道中心

### 9.3.1 渠道列表与创建

页面路径为 `/channels`。列表展示渠道名称、Provider、Base URL 主机、状态、健康、活动 Key 数、模型数、优先级、权重、最近测试、最近成功和最近错误。支持按 Provider、配置状态、健康状态和模型筛选。

创建渠道时配置 Provider 类型、名称、Base URL、可选代理、连接/读取/流空闲超时、默认非敏感请求头、优先级、权重和启用状态。Base URL 保存前进行协议、域名、解析地址和内部网络校验；默认禁止回环、链路本地、私网和云元数据地址，部署方显式允许的内网目标除外。

### 9.3.2 渠道详情与 Key 池

详情页路径为 `/channels/{id}`，包含概览、渠道 Key、上游模型、健康记录和调用记录。Key 列表仅展示名称、掩码、配置状态、健康状态、优先级、权重、可选 RPM/TPM、最后成功、最后错误和更新时间。

添加或轮换 Key 时原文通过受保护连接提交并立即加密。一个渠道可配置多份 Key；调度器先选择最高可用优先级，再在同优先级 Key 中按权重分配。Key 级 RPM/TPM、并发占用、冷却和健康必须参与选择，不能因某一 Key 限流而使整个渠道不可用。

### 9.3.3 检测与批量操作

渠道与单个 Key 均支持连接测试，测试可以选择指定模型和超时。结果包括成功状态、延迟、上游 request id、错误分类和时间，不保存消息正文。批量测试展示逐项结果和总进度，单项失败不回滚其他结果。

禁用、删除渠道或 Key 前必须进行引用影响分析。被活动路由引用的对象不能直接删除；禁用后若导致虚拟模型无路由，系统阻止发布或要求先完成替代配置。

## 9.4 模型与路由

### 9.4.1 上游模型目录

页面路径为 `/models/upstream`。模型记录绑定渠道，包含真实 model_id、显示名、能力、上下文长度、最大输出、支持参数、输入/输出价格、计价单位、币种、来源和状态。支持从渠道 API 同步、Adapter 预置导入或人工创建。

同步采用预览后提交，显示新增、变化、下线和冲突项，不自动覆盖人工锁定字段。价格和能力不完整的模型可以保存为禁用状态，但不能进入活动路由。启用模型前要求上下文、输出上限、流式能力和价格口径完整。

### 9.4.2 虚拟模型

页面路径为 `/models/virtual` 和 `/models/virtual/{id}`。虚拟模型 code 是业务应用请求中的稳定模型名，创建后不可随意修改。详情展示对外能力、已授权应用数、路由候选、健康、最近用量和变更历史。

虚拟模型对外能力取所有可切换候选的安全交集，或由管理员显式限制为更小范围。请求参数必须同时满足虚拟模型、应用策略和最终上游模型约束；不支持的显式参数在路由前拒绝，不允许静默删除后调用。

### 9.4.3 路由编辑

路由候选绑定渠道和上游模型，并配置优先级、权重、启用状态及可选的区域或环境条件。优先级数值越小越优先；同优先级按权重分流。权重为零的候选不接收正常流量，可用于人工检测。

路由编辑器实时检查重复候选、渠道/模型不匹配、无可用 Key、能力不一致、价格缺失、循环映射和应用影响。保存形成草稿，不直接影响运行。发布预览显示受影响虚拟模型和应用。

## 9.5 可靠性与配置发布

平台为每个请求设置总时限、单次上游时限和恢复预算。恢复顺序固定为：同 Key 的可重试请求重试、同渠道换 Key、同优先级换候选、进入下一优先级候选。每类动作有独立次数上限，所有动作受请求总时限约束。

渠道和 Key 按错误类别维护短期健康与熔断状态。上游认证失败使对应 Key 退出选择；429 按返回时间冷却；连续服务异常触发自动熔断；成功探测按规则恢复。运维可以人工摘除或恢复，但所有操作有版本校验和审计。

全局渠道、上游模型、虚拟模型和路由采用草稿—校验—发布。应用状态、应用密钥撤销和紧急人工摘除属于即时安全操作，不等待全局发布。配置发布页路径为 `/config/releases`，展示变更、校验、影响、发布进度、实例状态和回滚入口。

## 9.6 统一调用网关

V2.0 首期提供 OpenAI 兼容的 `GET /v1/models` 与 `POST /v1/chat/completions`，支持非流式和 SSE 流式。`/v1/models` 仅返回当前应用已授权且存在可用路由的虚拟模型；Chat 请求中的 model 使用虚拟模型 code。

应用通过 `Authorization: Bearer <application_key>` 认证。每个响应返回 `X-Request-Id`；调用方提供合法 `X-Request-Id` 时平台回传，否则生成。请求和响应协议对业务应用保持稳定，平台专有元数据放在明确的扩展字段中，不返回渠道 Key ID、上游凭证或内部配置内容。

非流式成功响应包含虚拟模型名、内容、finish_reason、Usage 和可选的 Trace 扩展。流式响应遵循连续 SSE chunk，并以单一 `[DONE]` 结束。流内错误只发送统一错误事件并关闭，不再发送成功终态。客户端取消必须向上游传播并及时释放容量和预占。

## 9.7 调用记录

调用记录路径为 `/calls`，支持按时间、应用、应用密钥、虚拟模型、渠道、上游模型、状态、错误码和 request_id 筛选。默认不保存消息正文；列表展示开始时间、应用、虚拟模型、最终渠道、状态、尝试数、输入/输出 Token、金额、首 Token 延迟和总耗时。

详情页路径为 `/calls/{requestId}`，以时间线展示准入、路由、每次 Attempt、恢复动作、流式提交、结算和终态。应用负责人只能查看所属应用；渠道 Key 只显示掩码名称或内部别名。诊断采样默认关闭，开启时必须脱敏、截断、限期保留并限制读取权限。

## 9.8 用量与成本

页面路径为 `/usage`，支持按应用、应用密钥、虚拟模型、上游模型、渠道和时间聚合。摘要展示请求、成功率、输入 Token、输出 Token、总 Token、金额、预算使用率和单位请求成本；趋势和分布图使用同一筛选口径。

成本按实际 Usage 和请求开始时锁定的价格快照计算。供应商未返回 Usage 时可以按明确算法估算并标记 `ESTIMATED`，不能与实际值混淆。不同币种默认分别展示，未配置可信汇率来源时禁止直接求和。免费模型金额为零，但 Token 和请求仍计入配额与统计。

额度流水路径为 `/usage/adjustments`，展示周期重置、人工调整、请求预占、实际结算和释放。面向应用负责人时隐藏平台内部技术字段，但必须能解释“为何被拒绝”和“本周期如何消耗”。

## 9.9 开发接入

开发接入页面优先从应用详情进入，路径为 `/applications/{id}/integration`。页面展示统一 Base URL、认证方式、可用虚拟模型、应用当前限制、Java/Python/JavaScript/cURL 示例和错误处理说明。示例中的密钥使用占位符，不把一次性密钥写进页面源代码或浏览器持久存储。

在线测试必须选择当前应用授权模型，并使用后台受控调用上下文；页面可以输入短测试消息，展示响应、Usage、耗时和 request_id。测试请求与正式请求使用同一准入、路由和结算链路，并明确标记调用来源。测试失败时展示统一错误和可操作建议。

## 9.10 审计与系统设置

审计日志路径为 `/audit`，记录管理身份、动作、目标、结果、request_id、时间以及变更前后摘要。密钥写入、轮换、撤销、额度调整、应用状态、模型授权、渠道状态、路由发布、回滚和权限变更必须审计。导出遵循当前数据权限和留存策略。

系统设置路径为 `/settings`，首期只包含企业身份适配、默认时区、日志留存、诊断采样、网络目标策略、预算告警阈值和运行默认值。会改变安全或成本口径的设置必须显示影响并要求高权限。任何设置页不得包含充值、支付、订阅或 C 端运营配置。

## 9.11 管理身份入口

未认证人员访问管理后台时进入 `/login` 或跳转至企业身份源。单一 OIDC/SAML 场景只展示企业标识、产品名称和“企业账号登录”主操作；多身份源场景展示部署方启用的登录方式。登录成功返回原目标页面，身份无角色时显示联系管理员的受限状态。

平台不提供自主注册、找回平台密码、短信验证码、社交账号登录和 C 端开户入口。身份源超时、回调参数无效、会话过期和权限不足分别显示可识别的错误与重试方式，不把身份故障渲染成系统空白页。退出后清除管理会话和敏感页面状态。

# 10 跨模块业务规则

## 10.1 应用策略层级

系统级安全上限高于应用策略，应用策略高于应用密钥策略。下层可以收紧模型、有效期、IP、RPM 和 TPM，不能扩大上层权限或容量。若上层策略变更导致下层配置超限，运行时使用更严格结果，页面标记冲突并要求修正。

## 10.2 Token 与金额预算

Token 预算按输入 Token 与输出 Token 之和结算，除非后续版本明确增加加权口径。金额预算按输入、输出各自价格计算。准入时根据请求和模型上限产生保守预占；成功后用实际值结算，失败或取消释放未使用部分。实际值超过预占时必须以原子方式补扣；余额不足的并发边界处理和最大透支保护由技术设计给出并通过并发测试证明。

预算周期支持生命周期、自然日、自然月和自定义起止。周期时区使用平台统一时区，页面明确显示。周期切换生成新策略版本，不能重写已关闭周期。人工调整必须使用幂等键，重复提交不能重复增加额度。

## 10.3 RPM 与 TPM

RPM 统计准入请求，TPM 统计预估与实际 Token。两者在集群共享状态中原子执行，窗口算法在技术设计中固定后不可由单实例自行解释。应用级和密钥级限制同时生效，响应提供标准 429、统一错误码、当前限制维度和可重试时间。

## 10.4 路由和 Key 负载均衡

路由先过滤配置禁用、健康不可用、应用无权限、能力不匹配、容量不足和价格缺失候选，再按优先级与权重选择。渠道 Key 使用相同的可用性过滤和优先级/权重原则。权重只在同一优先级内比较，不能跨优先级稀释主备关系。

单次请求一旦创建 Attempt，该 Attempt 只能使用一个渠道、一个真实模型和一份渠道 Key。切换必须创建新 Attempt。不得在日志中写出 Key 原文、认证头或完整 secret_ref。

## 10.5 配置生效

全局路由配置通过活动快照对新请求生效。应用状态、应用密钥和安全封禁即时生效；应用额度与速率修改对新准入即时生效；价格修改只影响新请求和新账本记录。每个 Trace 固定配置版本、应用策略版本和价格快照，以便复现。

## 10.6 删除与历史

业务主对象优先禁用或归档。存在历史调用、账本、审计或活动引用的应用、密钥、渠道和模型不得物理删除。允许删除的未使用草稿对象也必须校验版本和引用。历史记录使用快照名称，避免对象改名后无法解释。

## 10.7 统一错误

错误响应统一为 `error` 对象，至少包含 code、message、request_id、retryable 和可选 details。HTTP 状态与错误类别一致：请求校验 400、认证失败 401、无权限 403、对象不存在 404、版本或状态冲突 409、额度或速率限制 429、上游不可用 502/503、总超时 504。错误不得包含密钥、Token、认证头、数据库连接信息、堆栈或上游敏感正文。

# 11 管理后台信息架构与 UI 设计

## 11.1 导航结构

管理后台采用固定左侧导航和顶部上下文栏。一级导航顺序为：总览、应用中心、AI 资源、运行观测、开发接入、系统管理。AI 资源下包含渠道、上游模型、虚拟模型与路由、配置发布；运行观测下包含调用记录、用量与成本、额度流水。应用中心保持一级入口并位于资源配置之前。

应用详情是核心工作台。进入应用后，顶部显示应用切换器和当前环境，页签提供概览、密钥、模型、额度与速率、调用、用量、成员与审计。用户完成一个应用的日常管理时不应频繁跳到全局配置页面。

## 11.2 视觉方向

系统只设计简洁浅色主题。页面背景使用低对比浅灰，主要内容容器使用白色；主色使用克制的蓝色；成功、警告和错误色只表达真实状态。建议基础设计令牌如下，评审通过后由设计系统固定：

| 令牌 | 建议值 | 用途 |
|---|---|---|
| 页面背景 | `#F6F8FB` | 全局背景 |
| 内容背景 | `#FFFFFF` | 卡片、表格、表单 |
| 一级文字 | `#172033` | 标题与主要数据 |
| 二级文字 | `#667085` | 说明与辅助字段 |
| 边框 | `#E6EAF0` | 分隔与控件边界 |
| 主色 | `#2563EB` | 主按钮、链接和选中状态 |
| 成功 | `#16A34A` | 健康与成功 |
| 警告 | `#D97706` | 预算临界、降级 |
| 错误 | `#DC2626` | 不可用、失败和高风险操作 |

正文使用系统无衬线字体，中文优先 `PingFang SC`、`Microsoft YaHei`，数字使用等宽或表格数字特性。内容宽度适配 1280—1920 像素桌面，最小支持 1024 像素管理场景；本期不建设独立移动端。默认圆角 6—8 像素，阴影极弱或不用，依靠边框、留白和层级组织内容。

## 11.3 布局与组件

页面标题直接使用业务名称，如“应用”“渠道”“虚拟模型”“调用记录”。标题区右侧最多放一个主操作和必要次操作。统计卡只保留能驱动判断的指标，一行不超过四个；复杂数据优先表格，不用大量彩色卡片填充页面。

状态 Tag 仅用于 ACTIVE、DISABLED、异常、预算临界等真实状态。普通类型、名称和说明使用文本。列表统一提供搜索、筛选、排序、分页和刷新；筛选条件写入 URL，返回列表时保留。表格行点击进入详情，高风险操作放在明确菜单并二次确认。

表单采用单列或两列栅格，字段按业务分区。必填、单位、范围和继承关系在字段附近表达。高级参数默认折叠，但折叠标题显示已配置数量。保存按钮只在数据变化且校验通过时可用；并发冲突保留用户输入并展示最新版本差异。

## 11.4 页面状态

首次加载使用骨架或局部加载，不阻塞整个布局；刷新保留当前数据并显示轻量进度。空态说明为什么为空并给出唯一下一步，例如“尚未创建应用”；筛选无结果提供清空筛选。接口失败显示 request_id 和重试，权限不足显示 403 页面，不把用户导航到不存在的登录或首页。

操作成功必须对应真实服务结果。密钥写入成功、配置发布成功、额度调整成功和渠道检测成功分别展示准确反馈。异步发布或批量检测显示进度和最终逐项结果，不能在任务仍运行时提示完成。

## 11.5 敏感与高风险交互

一次性密钥弹窗禁止自动关闭，提供已复制确认，但用户关闭后不可恢复原文。禁用应用、撤销密钥、降低预算、摘除最后可用路由和回滚配置必须展示影响范围。页面不得把真实 Key、应用密钥、认证头、消息正文或数据库密码写入 URL、localStorage、错误详情、埋点和前端日志。

# 12 非功能需求

## 12.1 安全

管理后台接入企业可信认证，所有 API 后端执行 RBAC 和应用数据范围。应用密钥使用高熵随机值，持久化不可逆摘要；渠道 Key 使用信封加密或外部 Secret 引用，主密钥支持轮换。外部请求实施 SSRF 防护、TLS 校验、目标白名单和禁止危险重定向。

日志、Trace、审计、导出和指标默认移除认证头、Cookie、密钥、完整 secret_ref、数据库密码和消息正文。诊断采样默认关闭。所有敏感写入、额度调整、角色变更和发布操作都必须审计。

## 12.2 可用性与一致性

网关无可用路由、共享容量状态不可用、配置版本不一致或密钥服务不可用时必须明确拒绝，不静默绕过治理。请求、Reservation、Attempt、Usage Ledger 和审计写入使用 request_id 或业务幂等键避免重复。跨数据库与 Redis 的操作采用可恢复状态机，不宣称共同事务。

集群节点只处理已成功激活的完整配置版本。节点启动、就绪、下线和优雅关闭需暴露健康状态；关闭时停止新准入并等待运行请求至上限，然后取消和释放资源。

## 12.3 性能与容量

平台必须单独测量网关处理开销、外部模型耗时和流式首 Token 时间。RPM/TPM 与预算预占在目标并发下保持原子一致。具体吞吐量、可用性、P95 网关附加延迟和最大流式连接数需要结合首个部署规模确认，未确认前按第 14 章建议基线实施压测，不得宣称达到生产 SLA。

## 12.4 可观测性

每个请求拥有贯穿网关、日志、Trace 和响应的 request_id。核心指标包含请求数、成功率、错误分类、RPM/TPM、Token、金额、准入拒绝、路由分布、Key 使用、重试、故障切换、首 Token 延迟、总延迟和共享状态健康。指标标签禁止使用高基数密钥原文和消息正文。

## 12.5 兼容性与可维护性

业务 API 在同一主版本内保持向后兼容，新增响应字段不得破坏客户端。未知请求字段采用明确策略并文档化。Provider 协议差异封装在 Adapter，路由、预算、重试、Key 切换和结算由统一 Runtime 执行，不能散落在 Adapter。

# 13 验收要求

## 13.1 核心业务验收

| 验收 ID | 场景 | 可观察结果 |
|---|---|---|
| AC-APP-001 | 创建应用、授权模型、配置额度并签发密钥 | 应用详情显示完整接入信息，密钥仅显示一次 |
| AC-APP-002 | 应用调用 `/v1/models` | 只返回已授权且有可用路由的虚拟模型 |
| AC-GW-001 | 使用有效密钥同步调用 | 返回成功响应、Usage 和 request_id，Trace 正确归属应用 |
| AC-GW-002 | 使用有效密钥流式调用 | chunk 顺序正确，只有一个 `[DONE]`，取消后停止上游并释放资源 |
| AC-AUTH-001 | 无效、过期、撤销或跨 IP 密钥调用 | 请求在上游调用前被拒绝，错误无敏感信息 |
| AC-QUOTA-001 | Token 或金额预算耗尽 | 新请求原子拒绝，历史用量不被改写 |
| AC-RATE-001 | RPM 或 TPM 达到上限 | 返回 429、限制维度和 retry_after，其他应用不受影响 |
| AC-QUOTA-002 | 并发请求接近预算边界 | 不发生重复结算或无限透支，Reservation 均进入唯一终态 |
| AC-CHANNEL-001 | 同渠道一个 Key 429 | 在预算内换到健康 Key，Attempt 时间线可追踪 |
| AC-ROUTE-001 | 主渠道不可用 | 在流提交前切换候选；提交后不拼接其他模型输出 |
| AC-CONFIG-001 | 发布新路由 | 新请求使用新快照，运行中请求保持旧快照，失败时旧版本继续活动 |
| AC-COST-001 | 调用完成后查看成本 | 应用、密钥、虚拟模型、渠道、Token、币种和价格快照一致 |
| AC-SEC-001 | 检查日志、Trace、导出和浏览器存储 | 不出现应用密钥、渠道 Key、认证头和默认消息正文 |
| AC-PERM-001 | 应用负责人访问其他应用 | 页面与 API 均拒绝，产生越权审计 |
| AC-UI-001 | 1280 与 1920 桌面访问主要页面 | 无横向溢出，加载/空/错/无权限状态清晰，浅色层级一致 |

## 13.2 测试范围

后续实施必须覆盖前端 lint、typecheck、组件与页面测试、production build；后端单元测试、管理 API、统一网关、权限、事务、并发、流式和错误契约测试；数据库迁移、唯一约束、真实 PostgreSQL/MySQL 目标库测试；Redis 原子限流、预占、释放和故障恢复测试；真实 Provider 的同步与流式冒烟；从应用创建到用量结算的端到端测试。

测试数据不得写死绕过业务规则。真实密钥仅通过安全环境变量或交互式输入提供，不进入仓库、命令输出、测试报告或持久化明文文件。无法执行的生产门禁必须列明环境缺口和替代证据，不能以模拟测试代替真实结论。

# 14 重构交付与待确认事项

## 14.1 重构原则

本 PRD 是新的产品事实来源。现有代码可以作为协议转换、路由、流式、加密、数据库迁移和测试资产的候选复用来源，但不得因“已经实现”反向限制应用中心业务模型。实施前需要基于本 PRD 重新完成领域模型、信息架构、接口合同、数据模型和迁移策略设计。

首个可验收版本应按以下顺序形成闭环：应用与应用密钥；应用模型权限和 Token/金额/RPM/TPM；渠道多 Key 与上游模型；虚拟模型与路由；统一调用；用量成本与调用记录；浅色管理后台；发布、权限、安全和真实环境验证。任何阶段都必须保留可运行主链路，避免同时重写全部模块后才开始集成。

## 14.2 建议非功能验收基线

以下值用于启动架构评估，评审确认前属于建议方案：单实例网关附加延迟 P95 不高于 50ms（不含上游耗时）；目标可用性 99.9%；额度与账本重复扣减为 0；共享状态故障时治理绕过为 0；关键管理操作审计覆盖率 100%；上游和应用密钥泄漏为 0。目标吞吐量、最大并发流数和数据保留周期需按部署规模确认。

## 14.3 待确认事项

| 编号 | 决策项 | 建议方案 | 影响 |
|---|---|---|---|
| BP-001 | 首期企业身份接入方式 | 优先 OIDC，保留部署网关身份头适配 | 管理登录与成员同步 |
| BP-002 | 应用负责人是否可自主创建应用 | 默认允许申请/创建所属应用，生产启用可由管理员控制 | 权限和接入效率 |
| BP-003 | 是否强制应用至少配置 RPM 或 TPM | 建议生产应用至少配置一项，测试应用可使用平台默认 | 容量安全 |
| BP-004 | 预算周期与统一时区 | 默认自然月、Asia/Shanghai，可按应用覆盖 | 结算和重置 |
| BP-005 | 应用密钥轮换宽限期上限 | 建议不超过 24 小时 | 安全与上线切换 |
| BP-006 | 是否在首期支持密钥级独立预算 | 建议首期仅支持密钥级 RPM/TPM 和模型收紧，预算统一在应用级 | 数据模型和交互复杂度 |
| BP-007 | 支持的首批 Provider | 建议 OpenAI Compatible、OpenAI、Anthropic、Gemini、DeepSeek | Adapter 范围 |
| BP-008 | 成本基础币种与汇率 | 首期按原币种分别展示，不做自动换算 | 成本汇总 |
| BP-009 | Trace、账本、审计保留周期 | 由安全与成本部门分别确认 | 存储容量与合规 |
| BP-010 | 生产吞吐和流式并发目标 | 依据首批应用峰值乘容量冗余确定 | 架构、压测和部署 |
| BP-011 | 现有数据是否迁移 | 建议只迁移已验证的渠道、模型和价格，旧运行数据归档只读 | 重构迁移成本 |
| BP-012 | Embedded/Local Runtime 是否继续 | 建议不进入 V2.0 首期，待中心化链路稳定后评估 | 发布范围和维护成本 |

# 15 需求追溯

| 追溯 ID | 已确认需求来源 | 本 PRD 落点 | 核心验收 |
|---|---|---|---|
| TR-001 | 企业应用是每个业务接入点的管理单元 | 1.1—1.4、3.1—3.2、4.1、9.2 | AC-APP-001、AC-APP-002、AC-PERM-001 |
| TR-002 | 通过统一中台快速接入 AI | 1.2、4.1、9.6、9.9 | AC-GW-001、AC-GW-002 |
| TR-003 | 统一调度模型并进行负载均衡 | 4.3—4.6、9.4—9.6、10.4 | AC-CHANNEL-001、AC-ROUTE-001、AC-CONFIG-001 |
| TR-004 | 集中管理供应商 Key 和模型配置 | 3.1、4.2、9.3—9.5、12.1 | AC-CHANNEL-001、AC-SEC-001 |
| TR-005 | 按应用管理 Token、金额、RPM、TPM | 4.4—4.5、9.2.6、9.8、10.2—10.3 | AC-QUOTA-001、AC-QUOTA-002、AC-RATE-001、AC-COST-001 |
| TR-006 | 功能参考 New API，面向企业内部集成，不服务 C 端经营 | 1.2、1.5、9.10—9.11、附录 B | AC-AUTH-001、AC-PERM-001 |
| TR-007 | 管理后台采用简洁浅色 UI | 9.1—9.11、11.1—11.5 | AC-UI-001 |
| TR-008 | 推翻旧产品基线并清理过程文档 | 文档信息、14.1、协作规范 | 旧文档退出仓库，新实现只依据 V2.0 评审结果启动 |

# 附录 A 管理与业务 API 范围

## A.1 业务 API

| 方法与路径 | 说明 | 鉴权 |
|---|---|---|
| GET `/v1/models` | 返回应用可调用的虚拟模型 | 应用密钥 |
| POST `/v1/chat/completions` | 统一同步或流式文本对话 | 应用密钥 |

## A.2 管理 API

| 资源 | 建议路径范围 | 关键操作 |
|---|---|---|
| 应用 | `/admin/applications` | 列表、创建、详情、编辑、启停、归档 |
| 应用密钥 | `/admin/applications/{id}/keys` | 创建、轮换、禁用、撤销 |
| 应用模型 | `/admin/applications/{id}/models` | 授权、取消授权、参数收紧 |
| 应用额度 | `/admin/applications/{id}/quota` | 查看策略、调整、续期、重置 |
| 应用成员 | `/admin/applications/{id}/members` | 查看与维护授权关系 |
| 渠道 | `/admin/channels` | 创建、编辑、启停、影响分析、检测 |
| 渠道 Key | `/admin/channels/{id}/credentials` | 添加、轮换、启停、检测 |
| 上游模型 | `/admin/upstream-models` | 同步预览、导入、编辑、启停 |
| 虚拟模型 | `/admin/virtual-models` | 创建、编辑、授权影响、启停 |
| 路由 | `/admin/virtual-models/{id}/routes` | 候选新增、编辑、排序、启停 |
| 配置发布 | `/admin/config-releases` | 差异、校验、发布、状态、回滚 |
| 调用记录 | `/admin/calls` | 查询、详情、受控导出 |
| 用量成本 | `/admin/usage` | 摘要、趋势、分组、额度流水 |
| 审计 | `/admin/audit-logs` | 查询、详情、受控导出 |
| 接入信息 | `/admin/applications/{id}/integration` | 地址、模型、示例和在线测试 |

所有写接口使用版本或幂等键处理并发与重复提交。列表接口使用统一分页、排序和筛选格式；成功响应使用 `data`，失败响应使用统一 `error`。具体字段、OpenAPI、数据库 DDL 和事件合同在本 PRD 评审通过后另行设计，不作为新的过程文档提前固化。

# 附录 B 参考产品取舍

New API 的公开文档显示其通过统一接口聚合多个模型服务，并提供渠道、Token、模型限制、额度、日志与统计能力。轻享 AI 借鉴这些中台能力，同时把治理主体固定为企业应用，并排除普通用户注册、充值、订阅和返利等 C 端运营功能。

参考资料：

- [New API 功能概览](https://docs.newapi.pro/en/docs/guide/feature-guide/)
- [New API 渠道管理](https://docs.newapi.pro/zh/docs/guide/feature-guide/admin/channel)
- [New API API 密钥](https://docs.newapi.pro/zh/docs/guide/feature-guide/user/token)
- [New API 日志与统计](https://docs.newapi.pro/zh/docs/guide/feature-guide/admin/log)
