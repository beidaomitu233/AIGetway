# 轻享 AI 项目协作规范

## 项目定位与范围

轻享 AI（Light AI）是面向 Java 与 Spring Boot 应用的 AI 模型运行时。需求基线为 [轻享AI-产品需求说明书-PRD-V1.0.md](轻享AI-产品需求说明书-PRD-V1.0.md)。

V1.0 提供 Java SDK、Spring Boot Starter、Embedded Admin UI 和 Standalone Server，覆盖文本 Chat、Provider 适配、凭证池、模型别名、路由、容量控制、可靠性、调用观测和配置发布。

未经明确需求，不增加注册、充值、订阅、商业计费、多租户运营、Prompt 管理、知识库、MCP、工作流、Agent Runtime、多模态或模型训练功能。检索字段中的 application、project、tenant 不代表可以自行建设账户或租户体系。

## 基本原则与执行优先级

保持实现简单、清晰、可维护。优先完成明确需求，避免过度设计、冗余校验、额外兼容层和大规模测试体系。

执行优先级：

1. 用户当前明确需求及已确认约束。
2. PRD、已确认接口契约和现有项目架构。
3. 以最小改动范围完成需求。
4. 保证关键业务流程和数据一致性。
5. 保持代码清晰，并完成必要验证。

遇到多个可行方案，优先选择依赖更少、实现直接、维护成本更低的方案。技术栈与依赖版本按项目计划和实际构建文件执行，待确认选型不得描述为已确定或已实现。

## 任务范围与文档协作

仅要求需求分析、架构设计或执行计划时，只修改文档，不实现页面、接口或数据库迁移。用户明确要求实施功能时，按对应 Plan 执行；文档中的未勾选任务不构成自动开始全部开发的授权。

执行前读取当前任务相关文档：

| 文件 | 职责 |
|---|---|
| [PROJECT_DOCUMENT.md](PROJECT_DOCUMENT.md) | 项目范围、架构、权限、流程和公共协作规则 |
| [FRONTEND_PLAN.md](FRONTEND_PLAN.md) | 页面、组件、状态、接口依赖和前端验收 |
| [BACKEND_PLAN.md](BACKEND_PLAN.md) | API、DTO、运行服务、SDK和后端验收 |
| [DATABASE_PLAN.md](DATABASE_PLAN.md) | 实体字段、索引、约束、迁移与留存 |
| [COMMUNICATION.md](COMMUNICATION.md) | 待确认项、契约冲突、处理结论和验收证据 |

任务保持稳定编号与勾选框，按模块组成可独立验收的任务包，通常每包5—10项。任务须写清目标、输入输出、依赖、状态与异常、验收标准和必要测试。

需求缺失时可采用合理假设继续规划，并登记待确认项。实现遇到字段、接口或文档冲突时，记录具体位置、采用建议和受影响任务；继续处理无冲突部分，不私自扩大范围。已获用户确认的事项直接执行，不重复询问。

前端负责页面与接口消费，后端负责协议与业务服务，数据库负责迁移和索引。修改公共契约时同步更新受影响的 Plan，由相关执行方和审查方核对；未经验证不勾选完成。

## 架构与运行时约束

- Java SDK、Embedded 与 Standalone 共享统一协议和运行规则；公共客户端接口不暴露供应商 SDK 类型，也不依赖 Spring。
- Local Runtime 不连接管理数据库或配置发布服务；Standalone Client 只调用远程服务；Starter 按模式条件装配并允许宿主覆盖默认实现。
- Provider Adapter 负责单次外部调用、协议转换和错误分类。路由、重试、Credential Failover、Fallback、容量与熔断由 Runtime 统一执行。
- 配置编辑保存为草稿，经校验与发布后影响新请求。每个 Trace 固定一个配置快照，运行中请求保持原快照。
- Credential 密钥轮换、Standalone Access Credential 和人工熔断按各自契约即时生效；不得统一套用草稿发布流程。
- 流式响应首个业务块提交后禁止切换模型或拼接其他路径输出。取消、超时、背压和终止处理必须遵守统一协议。
- 集群容量使用共享原子状态，任何结束路径都需幂等结算和释放。共享状态不可用时按契约拒绝新预占，不静默退化为各实例独立计数。
- Trace、Attempt、响应 Usage、总消耗与费用按已确认口径记录；使用定点金额和明确币种，禁止跨币种直接求和。
- 不宣称数据库与 Redis 具有共同事务；跨存储操作按已确认方案处理幂等、故障恢复和状态收敛。

## 前端页面与文案

管理端服务于系统管理员、运维人员、开发人员和只读人员。页面保持清晰、简洁、美观，通过布局、间距、层级、字号和组件状态表达信息。

标题直接说明业务内容，例如“模型别名”“凭证池”“调用记录”“配置发布”。能通过标题、表格、表单或按钮表达的信息，不添加重复说明。

除非功能需要，不增加 Tag、Badge、辅助描述或填充文案。业务必要的配置启停、待发布变更、运行健康、加载失败和权限状态必须清楚展示。

交互结果与真实服务状态一致：“草稿已保存”“发布成功”“检测失败”“密钥已轮换”分别对应实际操作结果。模拟数据或未连接接口不得显示真实发布、写入或调用已成功。

用户页面不展示开发阶段、设计评审、内部实现和修改过程等元信息。涉及密钥一次显示、删除引用影响、版本冲突、留存清理、发布警告和流中断时，在相关操作位置给出必要提示。

列表支持计划规定的分页、筛选和排序；首次 loading、empty、error 与刷新状态分别处理。页面离开清理请求和轮询，快速筛选不能被旧响应覆盖。

界面中文与英文术语保持一致；仅在明确要求国际化时增加多语言能力。按任务要求的屏幕尺寸检查布局和溢出，不为未要求的设备额外建设独立页面。

## 安全与权限

- 分离管理身份、Standalone Access Token、Provider Credential 和内部实例身份，不交叉复用权限。
- 权限同时约束页面入口、API操作、应用数据范围及响应字段；隐藏按钮不能代替后端鉴权。
- Provider 密钥仅允许受保护写入和掩码读取；Access Token 只在签发或轮换响应中显示一次，持久化使用单向摘要。
- 密钥、Token、认证头、完整 secret_ref、连接密码和默认消息正文不得进入日志、Trace、审计、导出、示例或浏览器持久存储。
- 诊断采样默认关闭，启用后按配置脱敏、截断、限期保留；受控诊断读取必须鉴权并审计。
- Embedded Admin 缺少宿主认证时默认拒绝访问；显式本地访问规则按 PRD 执行，不增加默认管理员密码或匿名入口。

## 代码、注释与重构

代码、变量、界面文案、文档和配置只保留最终有效内容。名称直接表达业务对象，不添加“新版”“临时方案”等过程标签。待确认项与验收记录保存在协作文件中，不混入产品文案。

注释只解释必要业务规则、复杂逻辑或无法由代码表达的约束。避免重复代码本身的含义。

优先局部修改，保持已有模块边界、公共接口、调用链、目录、配置方式和数据结构稳定。仅当现有架构无法支持需求或局部扩展明显增加维护成本时扩大重构范围，不夹带无关整理。

数据库结构变更通过版本化迁移完成，不修改已发布迁移。字段变更同步核对 API、页面、历史数据、索引与留存规则。

## 测试与验收

测试覆盖当前需求、关键边界和明显回归。低风险文档或展示修改采用对应的内容、格式或布局检查，不建设重复测试体系。

运行时修改优先验证：

- Provider协议映射、参数与能力过滤、路由顺序和三类恢复预算。
- 容量原子预占、窗口结算、取消和超时后的单次释放。
- 流式提交前恢复、提交后中断、背压和取消传播。
- 草稿版本冲突、发布失败保护、快照原子激活和实例收敛。
- Trace与Attempt一致性、聚合幂等、历史价格和多币种归因。
- 四角色权限、敏感字段裁剪、密钥轮换与Token失效。

单元测试不能代替真实存储的一致性验证或性能压测。验证命令与环境以实际项目为准，报告实际结果，不把未执行测试描述为通过。

页面修改检查相关角色入口、必要状态、表单校验、数据显示与真实响应一致性，以及布局溢出。文档修改检查任务编号、交叉引用、路径、字段和 Git 格式。

## GitFlow 与任务交付

长期分支为 main 和 dev：main 保存稳定可发布版本，dev 保存完成集成的代码。功能分支从 dev 创建：

| 类型 | 分支命名 |
|---|---|
| 前端 | feature/frontend-模块名 |
| 后端 | feature/backend-模块名 |
| 数据库 | feature/database-模块名 |
| 架构与计划文档 | docs/architecture-plan |
| 发布 | release/<version> |
| 紧急修复 | hotfix/<name> |

功能完成并验证后合并回 dev。发布分支从 dev 创建，完成发布验证后合并 main 与 dev，并在 main 创建版本 Tag。紧急修复从 main 创建，验证后合并 main 与 dev。

每完成一个任务包提交一次 Git，一个 Commit 对应一个明确目的。格式示例：

- docs: update architecture plan
- feat(frontend): complete provider form
- feat(backend): complete model routing
- feat(database): add trace tables
- test: add routing tests
- fix: resolve review issue

交付顺序：运行相关验证 → 对照验收标准自检 → 勾选已完成任务 → 提交当前任务文件 → 记录必要的测试证据与协作结论 → 审查后集成。

不直接在 main 开发功能，不为整理历史做无必要重写，不提交无关文件、真实密钥或用户未要求处理的改动。推送远程、发布制品与部署按用户授权范围执行。

## 回复规范

回答简洁、直接、清晰，使用自然段落，按信息需要使用分点或表格。说明最终结果、关键变化、实际验证及未解决事项，避免长篇背景、重复总结和无关扩展。

禁用“不是……而是……”及类似对立式表达。不要把计划、Mock结果或文档检查描述为已完成业务实现。
