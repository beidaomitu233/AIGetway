# 轻享 AI 企业 AI 中台协作规范

## 权威基线

产品需求以 [轻享AI-企业AI中台-产品需求说明书-PRD-V2.0.md](轻享AI-企业AI中台-产品需求说明书-PRD-V2.0.md) 为唯一业务基线。

仓库现有源码形成于旧版需求，只能作为可复用技术资产评估，不能据此反推 V2.0 产品范围、接口或数据模型。V2.0 开始实现前，应先完成产品评审，再建立与新版 PRD 对应的架构、接口、数据迁移和分阶段交付方案。

## 产品边界

轻享 AI 是面向企业应用的统一 AI 接入与治理中台。企业应用是平台管理、授权、额度、限流、费用归属和观测的第一业务对象。

V2.0 首期必须形成以下闭环：

- 应用创建、应用密钥签发、模型授权和开发接入。
- 供应商渠道、上游 Key 池和上游模型统一管理。
- 虚拟模型、路由、权重负载均衡、重试、换 Key、故障切换和熔断。
- 应用级 Token 额度、金额预算、RPM、TPM、有效期及人工调整。
- OpenAI 兼容调用接口、调用记录、Token 用量、成本归属和审计。
- 简洁浅色的管理后台，优先呈现应用接入与运行治理。

首期不建设面向 C 端的注册、充值、订阅、支付、兑换码、邀请返利和公开售卖能力，也不扩展 Prompt 管理、知识库、工作流、Agent Runtime、模型训练和多模态接口。金额属于企业内部预算与成本核算口径。

## 执行原则

1. 执行优先级为：用户当前明确需求、已评审 PRD、已确认技术契约、最小可交付改动。
2. 用户只要求需求、设计或计划时，仅修改相应文档；用户明确授权开发后才修改产品代码和数据库。
3. 旧代码与新版需求冲突时，记录差异并提出保留、改造或移除建议，不默认保留旧行为。
4. 公共契约变更必须同步核对管理端、网关、数据模型、调用客户端、测试与迁移影响。
5. 未执行的验证不得描述为通过；模拟数据不得显示真实调用、发布、写入或计费成功。
6. 保持实现简单、清晰、可维护，不在当前需求外增加兼容层、业务模块或大规模重构。

## 核心业务约束

- 业务应用只持有平台签发的应用密钥，不接触上游供应商 Key。
- 应用密钥必须绑定应用、状态、有效期和可选独立限制；最终权限与限额取应用和密钥策略的更严格值。
- 请求在调用上游前完成应用身份、模型权限、Token 额度、金额预算、RPM、TPM 和并发准入。
- 路由目标必须明确真实渠道、上游模型、优先级、权重和故障策略。随机、轮询等算法只有在满足优先级、健康与容量条件的候选内执行。
- 流式响应首个业务块提交后不得切换模型或拼接其他路径输出。取消、超时和异常必须幂等释放容量并结算。
- 运行请求绑定确定的配置版本和价格快照。价格变更不改写历史成本。
- Token、金额和容量预占必须支持并发一致性。共享状态不可用时默认拒绝新准入，并完整记录原因。
- 删除被引用资源前必须检查依赖。调用、用量、费用和审计历史应保留业务快照，不因配置删除而失真。

## 安全与权限

- 分离管理身份、应用密钥、上游凭证和内部服务身份，不交叉复用权限。
- 管理后台和管理 API 均需鉴权；页面隐藏按钮不能替代服务端授权。
- 上游 Key 仅允许受保护写入和掩码读取。应用密钥原文只在创建或轮换成功时显示一次，持久化保存不可逆摘要。
- 密钥、认证头、数据库密码、完整请求正文及其他敏感信息不得进入日志、Trace、审计、导出、代码、文档和浏览器持久存储。
- 渠道地址与代理配置应防止 SSRF，限制危险协议、内网目标与异常重定向。
- 关键变更必须记录操作者、时间、对象、前后摘要和结果。

## 页面与文案

管理后台采用简洁浅色设计，使用清晰的留白、层级、表格和表单表达业务。应用中心是默认核心入口，应用详情聚合接入信息、可用模型、密钥、额度与限流、调用和用量。

标题与字段直接描述业务内容。无业务意义的渐变、装饰卡片、标签和说明文字应省略。loading、empty、error、disabled、partial-success 和刷新状态必须独立处理，结果文案必须与真实服务状态一致。

涉及密钥一次显示、轮换失效、额度清零、删除引用、发布警告和流中断时，应在操作位置给出明确提示。界面默认使用中文，协议名、模型名、RPM、TPM、Token 等行业术语保持一致。

## 测试与验收

验证应覆盖当前需求、关键边界和明显回归，重点包括：

- 从创建应用到使用应用密钥首次成功调用的完整链路。
- 应用停用、密钥撤销、模型越权、额度耗尽、金额超限、RPM/TPM 超限与到期拒绝。
- 多渠道和多 Key 的权重分配、失败重试、故障切换、熔断恢复与流式提交边界。
- 并发预占、成功结算、取消、超时、异常和重复回调下的单次释放。
- 调用记录、Attempt、Token、成本、配置版本、价格快照和审计的一致性。
- 系统管理员、平台运维、应用负责人和审计员的页面、API、数据范围与敏感字段裁剪。
- 页面首次加载、空数据、错误、刷新、筛选竞态、表单校验和常见桌面宽度布局。

单元测试不能代替真实数据库、共享容量存储、真实上游协议和必要性能场景的验证。测试报告应注明环境、命令、覆盖范围、结果和未执行项。

## GitFlow 与交付

长期分支为 `main` 和 `dev`。功能分支从 `dev` 创建，使用 `feature/frontend-*`、`feature/backend-*`、`feature/database-*` 或 `docs/*`；发布使用 `release/<version>`；紧急修复从 `main` 创建 `hotfix/<name>`。发布验证完成后合并回 `main` 和 `dev`，并在 `main` 创建版本 Tag。

一个 Commit 对应一个明确目的。提交前运行相关验证并核对变更范围，不提交真实密钥、临时产物、无关文件或未授权实现。推送远程、发布制品与部署按用户授权执行。

## 回复要求

回复应简洁说明最终结果、关键变化、实际验证和未解决事项。产品方案、Mock、文档检查和待评审内容必须使用准确状态，不描述为已完成业务实现。
