# Agent Rules

## 基本原则

保持实现简单、清晰、可维护。优先完成明确需求，避免为了潜在问题进行过度设计、过度测试和防御性构建。

仅实现当前功能真正需要的能力。除非需求明确要求，不主动增加复杂抽象、额外兼容层、冗余校验或大规模测试体系。

## 前端开发规范

前端页面保持清晰、简洁、美观，优先通过布局、间距、层级、字号、组件状态和视觉结构表达信息。

除非功能本身明确需要，请勿添加标签 Tag、Badge、说明性文字、冗余提示、辅助描述或为了填充页面而增加的文案。

页面设计应减少视觉噪音，避免重复表达已经可以通过标题、表格、表单、状态或布局理解的信息。

## 代码与文档规范

任何代码、注释、变量命名、界面文案、文档和配置文件中，只保留最终有效结果。

请勿添加开发过程、修改过程、历史方案、临时说明、对比版本或阶段性描述。

错误示例：

```text
青椒炒肉（不加东坡肉版本）
新接口（重构后的版本）
用户列表（新版）
临时解决方案
这里先这样处理
后续可以优化
```

正确写法应直接表达最终内容：

```text
青椒炒肉
用户列表
```

注释只用于解释必要的业务规则、复杂逻辑或无法通过代码本身清晰表达的约束。

## 修改与重构原则

功能完成后进行优化时，应优先采用局部修改。

除非现有架构已经无法支持需求，或者继续局部修改会明显增加维护成本，请勿进行大范围重构。

该原则同时适用于单模块和多模块项目。

优化时应尽量保持：

- 已有模块边界稳定。
- 已有公共接口稳定。
- 已有调用链稳定。
- 已有目录结构稳定。
- 已有配置方式稳定。
- 已有数据结构和数据库结构稳定。

新增需求优先通过扩展现有能力完成，避免因为局部优化引发跨模块连锁修改。

## 测试原则

测试以验证当前需求和关键业务路径为目标。

优先覆盖：

- 核心功能是否正确。
- 关键边界条件是否正确。
- 已修改逻辑是否存在明显回归。
- 核心接口和关键流程是否可以正常运行。

避免：

- 为低风险代码编写大量重复测试。
- 为极低概率场景增加复杂防御逻辑。
- 为当前不存在的需求提前建设测试体系。
- 因测试需要反向增加大量生产代码复杂度。

## 回复规范

回答保持简洁、直接、清晰。

使用陈述句和自然段落组织内容，根据问题复杂度适当使用分点和表格。

避免长篇背景铺垫、重复总结和无关扩展。

禁用“不是……而是……”及类似对立式表达。

涉及代码修改时，优先说明最终修改结果、关键变化和必要注意事项。

## GitFlow 工作规范

开发过程遵循 GitFlow。

长期分支：

```text
main
develop
```

`main` 保存稳定、可发布版本。

`develop` 保存已经完成集成、准备进入下一版本的代码。

功能开发从 `develop` 创建功能分支：

```text
feature/<name>
```

功能完成并验证后合并回 `develop`。

版本发布从 `develop` 创建：

```text
release/<version>
```

发布验证和必要修复完成后，将 release 分支合并到 `main` 和 `develop`，并在 `main` 创建版本 Tag。

线上紧急问题从 `main` 创建：

```text
hotfix/<name>
```

修复完成后合并回 `main` 和 `develop`。

提交应保持单一目的，一个 Commit 对应一个明确修改。

Commit Message 使用清晰、稳定的格式：

```text
feat: 增加模型路由能力
fix: 修复密钥池选择异常
refactor: 优化路由策略实现
docs: 更新部署说明
test: 增加路由核心测试
chore: 调整构建配置
```

避免将无关修改放入同一个 Commit。

避免直接在 `main` 上进行功能开发。

避免为了整理代码历史进行无必要的大范围重写。

## 执行优先级

执行任务时按照以下优先级处理：

1. 当前明确需求。
2. 已有项目架构和代码约束。
3. 最小范围完成需求。
4. 保证关键业务路径正确。
5. 保持代码清晰和长期可维护。
6. 在必要范围内进行优化和测试。

遇到多种可行方案时，优先选择改动范围更小、依赖更少、实现更直接、后续更容易维护的方案。
