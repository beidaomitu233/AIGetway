<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
  <modelVersion>4.0.0</modelVersion>

  <parent>
    <groupId>com.lightai</groupId>
    <artifactId>light-ai-parent</artifactId>
    <version>0.1.0-SNAPSHOT</version>
  </parent>

  <artifactId>light-ai-server</artifactId>
  <name>Light AI Server</name>
  <description>Standalone /v1 业务入口：模型目录、同步与流式 Chat、统一错误响应头；薄 HTTP 绑定层</description>

  <dependencies>
    <dependency>
      <groupId>com.lightai</groupId>
      <artifactId>light-ai-client</artifactId>
    </dependency>
    <dependency>
      <groupId>com.lightai</groupId>
      <artifactId>light-ai-spi</artifactId>
    </dependency>
    <dependency>
      <groupId>com.lightai</groupId>
      <artifactId>light-ai-runtime</artifactId>
    </dependency>
    <dependency>
      <groupId>com.lightai</groupId>
      <artifactId>light-ai-storage-jdbc</artifactId>
    </dependency>
    <dependency>
      <groupId>com.lightai</groupId>
      <artifactId>light-ai-admin</artifactId>
    </dependency>
    <dependency>
      <groupId>com.lightai</groupId>
      <artifactId>light-ai-provider-openai</artifactId>
    </dependency>
    <dependency>
      <groupId>com.lightai</groupId>
      <artifactId>light-ai-provider-anthropic</artifactId>
    </dependency>
    <dependency>
      <groupId>com.lightai</groupId>
      <artifactId>light-ai-provider-gemini</artifactId>
    </dependency>
    <dependency>
      <groupId>com.lightai</groupId>
      <artifactId>light-ai-provider-deepseek</artifactId>
    </dependency>
    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-web</artifactId>
    </dependency>
    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-jdbc</artifactId>
    </dependency>
    <dependency>
      <groupId>org.postgresql</groupId>
      <artifactId>postgresql</artifactId>
      <scope>runtime</scope>
    </dependency>
    <dependency>
      <groupId>com.mysql</groupId>
      <artifactId>mysql-connector-j</artifactId>
      <scope>runtime</scope>
    </dependency>
    <dependency>
      <groupId>com.h2database</groupId>
      <artifactId>h2</artifactId>
      <scope>runtime</scope>
    </dependency>
    <dependency>
      <groupId>io.micrometer</groupId>
      <artifactId>micrometer-core</artifactId>
    </dependency>

    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-test</artifactId>
      <scope>test</scope>
    </dependency>
  </dependencies>
  <build>
    <plugins>
      <plugin>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-maven-plugin</artifactId>
        <configuration>
          <mainClass>com.lightai.server.ServerApplication</mainClass>
        </configuration>
        <executions>
          <execution>
            <goals><goal>repackage</goal></goals>
          </execution>
        </executions>
      </plugin>
    </plugins>
  </build>

  <!-- 前端构建产物存在时，打包阶段自动并入 Server 静态资源（/ui/**）。 -->
  <profiles>
    <profile>
      <id>admin-ui</id>
      <activation>
        <file>
          <exists>${project.basedir}/../light-ai-admin-ui/dist/index.html</exists>
        </file>
      </activation>
      <build>
        <plugins>
          <plugin>
            <groupId>org.apache.maven.plugins</groupId>
            <artifactId>maven-resources-plugin</artifactId>
            <version>3.3.1</version>
            <executions>
              <execution>
                <id>copy-admin-ui</id>
                <phase>prepare-package</phase>
                <goals><goal>copy-resources</goal></goals>
                <configuration>
                  <outputDirectory>${project.build.outputDirectory}/static/ui</outputDirectory>
                  <resources>
                    <resource>
                      <directory>${project.basedir}/../light-ai-admin-ui/dist</directory>
                    </resource>
                  </resources>
                </configuration>
              </execution>
            </executions>
          </plugin>
        </plugins>
      </build>
    </profile>
  </profiles>
</project>
