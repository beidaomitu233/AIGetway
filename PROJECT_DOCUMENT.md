# 轻享 AI V2.1 产品优化与实施总文档

## 1. 文档定位

本文件是当前项目唯一实施总文档。业务基线仍为《轻享AI-企业AI中台-产品需求说明书-PRD-V2.0.md》；2026-09-14 用户确认的产品调整优先于其中冲突内容，后续实现、迁移和验收以本文件为执行入口。

本轮状态为“方案已形成，功能待实施”。现有代码可以用于迁移和复用，但旧页面、旧接口、旧表结构不能反向限制新的应用中心流程。

## 2. 本轮产品决策

### 2.1 系统管理只保留审计日志

系统管理菜单只保留“审计日志”。移除以下面向用户的功能：

- 待发布变更。
- 配置发布。
- 运行参数。
- 旧访问凭证。

对应前端页面、路由、权限项、接口、服务、仓储和数据库结构均进入删除范围。删除前先完成数据迁移和运行链路替换。

运行请求仍必须绑定不可变配置版本，用于并发一致性、调用追溯和成本快照。该能力改为系统内部机制：应用或渠道保存成功后生成新版本，并对后续请求自动生效，不再要求用户进入独立发布页面。

审计日志及其数据库历史完整保留。应用、渠道、映射、风险策略、密钥、额度和状态变更都写审计。

### 2.2 风险控制集中在一个页面

新增一级功能“风险控制”，使用一个页面完成三类策略配置，不拆成三个菜单。页面按三个区块或页签组织：

1. 关键词屏蔽：维护启停、匹配方式、关键词组、作用范围和命中动作。
2. 异常消耗：控制应用在短时间内突增的请求量、Token 或金额消耗。
3. 应用白名单：决定未加入白名单的应用是否允许调用。

页面顶部显示总开关、当前生效版本和最近命中数量；页面底部统一保存。高风险变更展示影响范围，保存成功后自动生成策略版本并写审计。

MVP 规则如下：

| 策略 | 最小配置 | 默认动作 |
| --- | --- | --- |
| 关键词屏蔽 | 关键词、精确/包含匹配、全局或指定应用、是否忽略大小写 | 调用上游前拒绝 |
| 异常消耗 | 时间窗口、请求数/Token/金额阈值、阻断时长、作用范围 | 拒绝并记录风险事件 |
| 应用白名单 | OFF/仅记录/强制阻断、允许的应用集合 | 强制模式下拒绝非白名单应用 |

关键词命中只记录规则 ID、应用、时间、动作和 request_id，不保存命中的完整请求正文。异常消耗使用 Redis 原子窗口统计；共享状态不可用时按现有安全原则拒绝新准入。白名单校验在上游调用前完成。

### 2.3 模型配置收回应用工作台

渠道只负责上游连接：

- Provider/协议类型。
- Base URL 和网络参数。
- 上游 Key 池。
- 连接检测和健康状态。
- 可选的实时模型目录查询能力。

不再要求管理员把供应商的上百个模型逐个持久化成“上游模型”记录。渠道支持按需从供应商模型接口查询、搜索和分页；不能查询的渠道允许直接输入真实模型名。

应用详情新增“模型映射”页签。每条映射表达：

`业务请求模型名 → 一个或多个真实调用目标`

映射包含：

- 对外模型名：业务应用在 OpenAI 兼容请求中提交的 `model`。
- 目标渠道。
- 上游真实模型名。
- 优先级、权重和启停状态。
- 可选的超时、重试和能力限制覆盖。
- 当前版本和更新时间。

同一对外模型名可以配置多个目标。相同优先级内按权重负载均衡，不同优先级形成主备；失败时按策略换 Key 或切换目标。流式响应首个业务块提交后禁止切换。

应用级映射支持两种创建方式：

- 单条创建：手工输入对外模型名并选择渠道、真实模型。
- 批量创建：从渠道实时模型目录批量选择，默认生成“同名映射”，随后可修改对外名称或合并为多目标映射。

“替换配置”采用完整替换语义：提交一组映射时服务端校验版本、引用、渠道健康和重复模型名，通过后创建不可变应用配置版本，并原子切换当前版本。运行中的请求继续使用旧版本，新请求使用新版本。历史 Trace 保存对外模型名、渠道、真实模型名和配置版本快照。

旧“虚拟模型与路由”概念在产品界面统一改名为“模型映射”；旧全局虚拟模型权限关系迁移为应用自己的映射。

### 2.4 修复新建应用不可保存

当前缺陷根因已确认：新建表单默认开启 Token、金额、RPM、TPM 四个限制，但对应数值为空，`formInvalid` 始终为真；保存按钮直接禁用，又没有集中说明未满足的条件。

修复要求：

- 创建应用只要求应用名称、编码、负责人和环境。
- Token、金额、RPM、TPM 默认关闭限制，或由平台提供明确有效的默认值。
- 币种只在启用金额限制时必填。
- 模型映射不是创建应用的前置条件，可在创建成功后配置。
- 保存按钮只在提交中、无权限或页面关键数据未加载时禁用。
- 用户点击保存后执行 Ant Design 表单校验，定位并滚动到首个错误字段。
- 删除二次“检查并保存”步骤，普通创建使用一次保存；高风险设置单独确认。
- 后端允许可选限额为 null，并为缺省状态建立明确契约。
- 增加最小创建、带限额创建、无权限、重复编码和参数错误测试。

该问题作为第一优先级修复，完成后才能开展后续人工验收。

## 3. 目标菜单与页面

左侧菜单精简为七个业务入口：

| 分组 | 菜单 | 主要内容 |
| --- | --- | --- |
| 概览 | 运行概览 | 请求、成功率、Token、成本和异常摘要 |
| 接入管理 | 应用 | 应用、密钥、模型映射、额度、调用与接入信息 |
| 接入管理 | 渠道 | 连接、Key 池、实时模型查询和健康检测 |
| 安全治理 | 风险控制 | 关键词、异常消耗、应用白名单 |
| 运行观测 | 调用记录 | Trace、Attempt、路由路径和错误 |
| 运行观测 | 用量与成本 | Token、金额、额度调整和趋势 |
| 系统管理 | 审计日志 | 管理操作和风险策略变更审计 |

以下独立菜单取消并吸收到业务对象中：

- 上游模型：并入渠道详情的实时模型目录。
- 虚拟模型与路由：并入应用详情的模型映射。
- 限流策略：应用限额进入应用详情；渠道容量进入渠道详情。
- 可靠性策略、熔断状态：进入渠道和映射目标的高级设置与运行状态。
- 额度流水：并入用量与成本。
- 待发布变更、配置发布、运行参数、旧访问凭证：删除。
- 开发接入：并入应用详情“接入信息”。

默认入口仍为应用列表；创建应用成功后进入应用详情，并引导“添加渠道或选择已有渠道 → 配置模型映射 → 创建应用密钥 → 调用验证”。

## 4. 目标接口

### 4.1 应用与映射

- `POST /admin/applications`：创建最小应用，限额和映射均可选。
- `GET /admin/applications/{id}/mappings`：读取当前映射及版本。
- `PUT /admin/applications/{id}/mappings`：按版本完整替换映射集合。
- `POST /admin/applications/{id}/mappings:validate`：保存前校验重复名称、目标和能力。
- `POST /admin/applications/{id}/mappings:bulk-create`：根据渠道模型目录生成批量草案。
- `GET /admin/applications/{id}/models`：返回应用当前可调用的对外模型名。

### 4.2 渠道模型目录

- `GET /admin/channels/{id}/model-catalog?query=&cursor=`：按需查询供应商模型目录。
- 查询结果不要求全部落库，只保留短时缓存和本次选择。
- 渠道不支持模型列表接口时返回 `manual_input_allowed=true`。

### 4.3 风险控制

- `GET/PUT /admin/risk-control/policy`：读取或替换当前风险策略版本。
- `GET /admin/risk-control/applications`：白名单应用候选。
- `GET /admin/risk-control/events`：风险命中记录，可按应用、类型和时间筛选。
- 风险事件详情不得返回完整提示词或凭证明文。

### 4.4 删除接口

旧上游模型、全局虚拟模型、候选路由、草稿发布、运行参数和旧访问凭证管理接口在迁移期标记弃用。前端切换且运行链路改造完成后删除，不能长期保留两套写入口。

## 5. 数据库调整

新增核心表：

| 表 | 用途 |
| --- | --- |
| `application_model_mapping` | 应用对外模型名、状态和当前版本 |
| `application_model_target` | 映射目标的渠道、真实模型名、优先级、权重和策略 |
| `application_config_revision` | 不可变应用配置版本和激活记录 |
| `risk_policy_revision` | 不可变风险策略版本 |
| `risk_keyword_rule` | 关键词规则 |
| `risk_application_whitelist` | 应用白名单 |
| `risk_event` | 风险命中事实记录 |

迁移原则：

1. 新增迁移文件，不修改已执行的 V1—V8。
2. 由现有应用模型权限、虚拟模型和候选路由生成应用映射。
3. 保留 Trace、Attempt、Usage、Ledger 和审计中的历史快照字段。
4. 对迁移结果执行应用数、映射数、目标数和可调用模型对账。
5. 管理端与网关全部切换到新结构后，再通过后续迁移删除旧表和外键。

计划删除的旧业务结构包括：`upstream_model`、模型同步任务表、`virtual_model`、`route_candidate`、旧应用模型权限关联、旧访问凭证表、草稿/发布记录及其结果表、运行参数管理表。运行所需的配置版本由 `application_config_revision` 替代；审计、调用、用量、额度、应用密钥、渠道和渠道 Key 表保留。

如果旧表仍被调用历史外键引用，先把历史实体引用转为可空或快照字段，再删除配置表，不能级联删除历史记录。

## 6. 网关执行顺序

请求处理顺序固定为：

1. 验证应用密钥、状态、有效期和 IP。
2. 校验应用白名单策略。
3. 执行关键词规则。
4. 执行异常消耗窗口校验。
5. 校验应用额度、预算、RPM、TPM 和并发。
6. 按请求模型名读取应用当前映射版本。
7. 过滤不可用目标并按优先级、权重选择渠道和真实模型。
8. 从渠道 Key 池选择凭证并调用上游。
9. 完成结算、Trace、Attempt、风险事件和审计记录。

任一步骤拒绝都不得调用上游。重试、换 Key、切换目标必须创建可追踪 Attempt；流式提交后不再切换。

## 7. 实施任务包

### 7.0 领取记录

| 任务包 | 负责人 | 领取时间 | 文件范围 | 状态 |
| --- | --- | --- | --- | --- |
| P0：恢复可用性与菜单收口 | 代码审查与修复模型/root | 2026-09-15 | `light-ai-admin-ui/src/pages/applications/ApplicationFormPage.vue`、`light-ai-admin-ui/src/app/navConfig.ts`、相关测试、应用创建后端契约与 V9 迁移 | 已验证 |
| P1：应用模型映射数据与后端 | 代码审查与修复模型/root | 2026-09-15 | light-ai-admin, light-ai-client, light-ai-runtime, light-ai-storage-jdbc 及相关测试 | 待合并 |
| P2：应用工作台与渠道页面 | 代码审查与修复模型/root | 2026-09-15 | light-ai-admin-ui 应用详情、渠道详情、接入示例、相关测试 | 待复验 |
| P3：风险控制单页与准入校验 | 代码审查与修复模型/root | 2026-09-15 | light-ai-admin-ui 风险控制页、light-ai-admin 风险策略 API、light-ai-runtime 准入校验、Redis/数据库迁移及相关测试 | 待复验 |

### P0：恢复可用性与菜单收口

- 修复新建应用保存按钮和默认值。
- 删除二次确认步骤并补齐字段错误提示。
- 调整左侧菜单为目标七入口。
- 将开发接入入口移入应用详情。
- 完成前端 typecheck、组件测试和应用创建 API 测试。

### P1：应用模型映射数据与后端

- 新增应用映射、目标和配置版本表。
- 实现映射查询、校验、完整替换和批量创建接口。
- 实现渠道实时模型目录适配，百炼等大目录使用搜索和分页。
- 将 Runtime 路由读取切换到应用映射版本。
- 迁移现有虚拟模型和候选路由数据并完成对账测试。

P1 当前交付记录（2026-09-15）：

- V10 已加入 MySQL、PostgreSQL/H2 迁移，新增应用映射、目标和配置版本表；旧应用模型权限与候选路由按原 ID 回填。
- 管理端已实现映射查询、校验、完整替换、批量草案和渠道目录接口；应用密钥鉴权会读取当前激活映射并拒绝映射表读取失败时的静默放宽。
- 替换操作保留禁用历史行，写入真实映射版本快照；当前查询仅返回激活映射和目标。
- 已通过 `mvn -B -pl light-ai-admin -am test`（243 项）、`mvn -B -pl light-ai-storage-jdbc -am test`（58 项）和运行时回归（81 项）；新增 H2 映射替换/版本快照测试通过。
- 渠道目录已接入 ProviderAdapter.listModels；不支持实时目录或缺少凭证时返回落库目录并标记 manual_input_allowed，批量草案支持瞬时目录条目。当前仅以 Stub Adapter/H2 验证，真实供应商同步、真实页面和新建手工映射后的网关调用仍待环境复验，P1 不宣称整条业务链路已通过。

### P2：应用工作台与渠道页面

P2 当前交付记录（2026-09-15）：

- 应用详情已移除旧的模型授权弹窗，改为模型映射工作区，支持现有映射编辑、优先级/权重/启停/策略 JSON、完整替换前校验和变更原因审计。
- 应用映射支持从渠道目录批量生成草案；草案不会直接写入，保存时调用 `mappings:validate` 和版本化 `PUT /mappings`。
- 渠道详情已提供实时模型目录搜索，前端按 `model_name`、`manual_input_allowed` 等真实协议字段渲染；不支持实时目录时提示在应用映射中手工输入模型名。
- 接入示例和应用文案已统一为“模型映射”。旧模型授权代码和弹窗已从应用详情移除。
- 前端 `npm run typecheck`、`npm run build`，以及应用/渠道回归测试 49 项通过；后端映射、渠道目录和 HTTP 契约测试通过。
- 仍待真实供应商、真实凭证和网关运行环境复验；当前 ProviderAdapter/H2 验证不能证明真实上游同步或新建手工映射已完成调用链路，故状态为“待复验”。

- 应用详情新增模型映射页签和批量选择流程。
- 渠道详情新增实时模型查询，不再提供独立上游模型 CRUD。
- 将权重、主备、重试和熔断高级设置放入映射目标。
- 更新接入示例和 `/v1/models` 展示。
- 完成真实渠道的大目录、同名映射和多目标路由测试。

### P3：风险控制

- 新增风险策略、关键词、白名单和风险事件数据结构。
- 实现 Redis 异常消耗原子窗口。
- 在网关准入链路接入三类风险校验。
- 实现单页风险控制和风险事件查询。
- 覆盖关键词命中、阈值边界、非白名单、Redis 不可用和权限测试。

P3 当前交付记录（2026-09-15）：

- V11 已加入 MySQL、PostgreSQL/H2 风险策略迁移，包含当前策略、不可变版本快照、关键词规则、应用白名单和风险事件表；策略替换清理旧关联行并写入版本快照。
- 管理端提供 `GET/PUT /admin/risk-control/policy`、应用候选和带应用/类型/时间筛选的风险事件接口；写入受 `risk-control.manage` 和版本 CAS 保护，所有变更写审计。
- 风险控制页将关键词、短时请求/Token/金额阈值、白名单和命中事件集中在同一页，支持全局或指定应用关键词规则；菜单与权限矩阵已同步。
- 网关同步/流式调用在路由和上游调用前执行关键词、白名单和异常消耗准入；Redis 原子窗口实现已接入独立服务，未装配共享窗口时使用嵌入式窗口。共享状态或策略读取失败按 fail-closed 拒绝请求。
- 已通过完整 `mvn -B -pl light-ai-server -am test` 回归（520 项，失败 0，错误 0，跳过 2 项 Redis IT），并通过风险准入 H2 回归（关键词、Token/金额阈值和事件记录）、迁移/SchemaGuard、角色权限测试；前端 typecheck、build、风险控制页和布局回归通过。
- 真实 Redis 不可用故障、真实管理身份权限和真实供应商上游链路仍待运行环境复验，因此 P3 状态保留“待复验”。

### P4：删除旧模块

- 删除旧上游模型、全局虚拟模型与候选路由前后端。
- 删除草稿发布、运行参数和旧访问凭证前后端。
- 删除旧权限项、路由、服务装配、仓储和不再需要的表。
- 保留并回归审计、调用历史和成本快照。
- 完成 PostgreSQL、MySQL 与全新数据库迁移测试。

### P5：端到端复验

- 创建应用。
- 配置渠道与 Key。
- 在应用中批量或手工建立模型映射。
- 创建应用密钥并完成同步、流式调用。
- 验证权重负载均衡、换 Key、故障切换和映射替换。
- 验证三类风险策略均在调用上游前生效。
- 核对调用、Token、成本、配置版本和审计一致。

## 8. 验收标准

- 用户可以只填写最小信息创建应用，保存按钮不会无解释地保持灰色。
- 面对上百个供应商模型时，无需逐个创建上游模型记录。
- 应用负责人能在应用详情完成模型映射、权重和主备配置。
- 替换映射后，新请求使用新版本，运行中请求和历史记录保持原版本。
- 风险控制三类策略可在一个页面配置并查看命中结果。
- 非白名单、关键词命中和异常消耗请求在上游调用前拒绝。
- 左侧菜单只保留七个明确业务入口，系统管理仅有审计日志。
- 旧接口、代码和数据库表完成迁移后删除，不保留双写。
- 应用密钥、渠道 Key、请求正文和敏感匹配内容不进入日志或审计。
- 核心链路通过真实数据库、Redis 和至少一个真实供应商的同步与流式验证。
